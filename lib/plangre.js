var ruby = require('./ruby.js'),    checker = require('./checker.js'),    tokenizer = require('./tokenizer.js');function analyze(text) {    var tokens = tokenizer.getTokens(text);    var check = checker.check(text, tokens, ruby);    var max = 0;    var result = null;        for (n in check) {        if (check[n] > max) {            max = check[n];            result = n;        }    }        return result;}module.exports = {    analyze: analyze}